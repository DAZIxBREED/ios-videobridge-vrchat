# iOS VideoBridge Package

Runtime and editor code for the Phase 1 Unity reference player.

Namespace: `DAZIxBREED.IOSVideoBridge`

The package is embedded in the repository. Unity detects packages placed directly under the project's `Packages` directory; no local `file:` dependency is required in the project manifest.
